export default function Home() {
  /**
   * Description placeholder
   *
   * @param {string} params
   * @returns {boolean}
   */
  const name = (params: string) => {
    return true;
  };

  return (
    <main>
      <div>Hello world!</div>
    </main>
  );
}
